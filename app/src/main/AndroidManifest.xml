<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.varma.numbercricket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutActivity"></activity>
        <activity android:name=".TestActivity" />
        <activity android:name=".EndActivity" />
        <activity android:name=".PlayActivity2" />
        <activity android:name=".PlayActivity1" />
        <activity android:name=".FactorActivity2" />
        <activity android:name=".FactorActivity1" />
        <activity android:name=".StyleActivity" />
        <activity android:name=".BetActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>